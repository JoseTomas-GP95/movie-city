import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  
  root: {
    maxWidth: 400,
    maxHeight: '100%',
    marginTop: '12%',
    marginBottom: '12%',
    zoom: '60%'
  },
  media: {
    width: 'auto',
    height: '100vh'

  },

}));
